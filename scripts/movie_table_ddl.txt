CREATE TABLE "MOVIE" 
   (	"MOVIE_ID" NUMBER, 
	"TITLE" VARCHAR2, 
	"BUDGET" NUMBER, 
	"GROSS" NUMBER, 
	"LIST_PRICE" NUMBER, 
	"GENRES" JSON, 
	"SKU" VARCHAR2, 
	"YEAR" NUMBER, 
	"OPENING_DATE" DATE, 
	"VIEWS" NUMBER, 
	"CAST" JSON, 
	"CREW" JSON, 
	"STUDIO" JSON, 
	"MAIN_SUBJECT" VARCHAR2, 
	"AWARDS" JSON, 
	"NOMINATIONS" JSON, 
	"RUNTIME" NUMBER, 
	"SUMMARY" CLOB) ;

CREATE UNIQUE INDEX "ADMIN"."PK_MOVIE_ID" ON "ADMIN"."MOVIE" ("MOVIE_ID") 
  ;

ALTER TABLE "ADMIN"."MOVIE" ADD CONSTRAINT "PK_MOVIE_ID" PRIMARY KEY ("MOVIE_ID")
  USING INDEX "ADMIN"."PK_MOVIE_ID"  ENABLE;

ALTER TABLE MOVIE 
    ADD CONSTRAINT PK_MOVIE_ID PRIMARY KEY ( MOVIE_ID ) 
    USING INDEX PK_MOVIE_ID ;
